version: 1.0
failThreshold: 100
profile:
  name: qodana.recommended
inspections:
  LicenseAudit:
    paths:
      - "gradle_kts/"
      - "npm/"

    dependencyScopes:
      - type: "gradle"
        scopes:
          - "runtime"
          - "runtimeClasspath"
